const login = '/login';
const signup = '/signup';
const forgotPassword = '/forgot-password';
const resetPassword = '/reset-password';

const home = '/';
const support = '/support';

export const urls = {
  login,
  signup,
  forgotPassword,
  resetPassword,
  home,
  support
};
